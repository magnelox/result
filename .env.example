# Database Connection (SQLite for fast dev, PostgreSQL for production)
DATABASE_URL="file:./dev.db"

# Redis Cache & Rate Limiter (Optional - memory fallback included if unset)
REDIS_URL="redis://localhost:6379"

# Authentication Secrets
JWT_SECRET="ssu_super_secret_admin_jwt_key_2026_change_in_production"
ADMIN_SESSION_NAME="ssu_admin_session"

# Grade Card Storage Directory
GRADE_CARD_STORAGE_PATH="./storage/grade-cards"

# Environment
NODE_ENV="development"
PORT=3000
